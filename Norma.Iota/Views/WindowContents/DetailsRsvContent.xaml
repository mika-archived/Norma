<UserControl x:Class="Norma.Iota.Views.WindowContents.DetailsRsvContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:actions="clr-namespace:Norma.Eta.Actions;assembly=Norma.Eta"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:metro="http://schemes.grabacr.net/winfx/2014/controls"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:properties="clr-namespace:Norma.Eta.Properties;assembly=Norma.Eta"
             xmlns:vm="clr-namespace:Norma.Iota.ViewModels.WindowContents"
             d:DataContext="{d:DesignInstance vm:DetailsRsvContentViewModel}"
             d:DesignHeight="250"
             d:DesignWidth="300"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrushKey}" />
        </Style>
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <prism:InteractionRequestTrigger SourceObject="{Binding WindowCloseRequest, Mode=OneWay}">
            <actions:WindowCloseAction />
        </prism:InteractionRequestTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" metro:MetroWindow.IsCaptionBar="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Margin="10,0,0,0"
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI Light"
                           FontSize="18"
                           Foreground="{DynamicResource ForegroundBrushKey}"
                           Text="{Binding WindowTitle}"
                           TextTrimming="CharacterEllipsis" />
                <metro:SystemButtons Grid.Column="1" VerticalAlignment="Top" />
            </Grid>
        </Border>
        <Grid Grid.Row="1">
            <TabControl Background="{DynamicResource BackgroundBrushKey}" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabItem}">
                                    <ContentPresenter x:Name="ContentSite"
                                                      Margin="10,5"
                                                      ContentSource="Header" />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style x:Key="HeaderTextBlockStyle" TargetType="{x:Type TextBlock}">
                        <Setter Property="FontFamily" Value="Segoe UI" />
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="FontWeight" Value="Normal" />
                        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrushKey}" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                <Setter Property="FontWeight" Value="Medium" />
                                <Setter Property="Foreground" Value="{DynamicResource ActiveForegroundBrushKey}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource ActiveForegroundBrushKey}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TabControl.Resources>
                <TabItem>
                    <TabItem.Header>
                        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="{x:Static properties:Resources.RsvByTime}" />
                    </TabItem.Header>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="FontFamily" Value="Segoe UI" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="Foreground" Value="{DynamicResource ActiveForegroundBrushKey}" />
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                            </Grid.RowDefinitions>
                            <!--#region StartTime-->
                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="{x:Static properties:Resources.StartAt}" />
                            <metro:PromptTextBox Grid.Row="0"
                                                 Grid.Column="1"
                                                 Width="120"
                                                 Height="30"
                                                 Margin="5"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Center" />
                            <!--#endregion-->
                            <!--#region RepetationType-->
                            <TextBlock Grid.Row="1"
                                       Grid.Column="0"
                                       Text="{x:Static properties:Resources.RepeatInterval}" />
                            <metro:PromptComboBox Grid.Row="1"
                                                  Grid.Column="1"
                                                  Width="120"
                                                  Height="30"
                                                  Margin="5"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  IsEditable="False" />
                            <!--#endregion-->
                            <!--#region Range-->
                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="{x:Static properties:Resources.ExpiredAt}" />
                            <metro:PromptTextBox Grid.Row="2"
                                                 Grid.Column="1"
                                                 Width="120"
                                                 Height="30"
                                                 Margin="5"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Center" />
                            <!--#endregion-->
                        </Grid>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Margin="5"
                                    Content="{x:Static properties:Resources.Ok}" />
                            <Button Grid.Column="1"
                                    Margin="5"
                                    Command="{Binding CancelCommand}"
                                    Content="{x:Static properties:Resources.Cancel}" />
                        </Grid>
                    </Grid>
                </TabItem>
                <TabItem>
                    <TabItem.Header>
                        <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="{x:Static properties:Resources.RsvByKeyword}" />
                    </TabItem.Header>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.Resources>
                                <Style TargetType="{x:Type TextBlock}">
                                    <Setter Property="FontFamily" Value="Segoe UI" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="Foreground" Value="{DynamicResource ActiveForegroundBrushKey}" />
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                                <RowDefinition Height="30" />
                            </Grid.RowDefinitions>
                            <!--#region Keyword-->
                            <TextBlock Grid.Row="0"
                                       Grid.Column="0"
                                       Text="{x:Static properties:Resources.Keyword}" />
                            <metro:PromptTextBox Grid.Row="0"
                                                 Grid.Column="1"
                                                 Width="120"
                                                 Height="30"
                                                 Margin="5"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Center" />
                            <!--#endregion-->
                            <!--#region IsRegexMode-->
                            <CheckBox Grid.Row="1"
                                      Grid.Column="0"
                                      Grid.ColumnSpan="2"
                                      Margin="5"
                                      VerticalAlignment="Center"
                                      Content="{x:Static properties:Resources.IsRegex}" />
                            <!--#endregion-->
                            <!--#region Range-->
                            <TextBlock Grid.Row="2"
                                       Grid.Column="0"
                                       Text="{x:Static properties:Resources.ExpiredAt}" />
                            <metro:PromptTextBox Grid.Row="2"
                                                 Grid.Column="1"
                                                 Width="120"
                                                 Height="30"
                                                 Margin="5"
                                                 HorizontalAlignment="Left"
                                                 VerticalAlignment="Center" />
                            <!--#endregion-->
                        </Grid>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Margin="5"
                                    Content="{x:Static properties:Resources.Ok}" />
                            <Button Grid.Column="1"
                                    Margin="5"
                                    Command="{Binding CancelCommand}"
                                    Content="{x:Static properties:Resources.Cancel}" />
                        </Grid>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>

